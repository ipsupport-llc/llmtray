{
  "repo": "ipsupport-llc/mlx-lm",
  "pinned_ref": "23aa4d0e25c83565147df968cede6791483b6ab1",
  "_comment": "Pinned commit of our own mlx-lm fork's main branch -- installed via 'pip install git+https://github.com/ipsupport-llc/mlx-lm.git@<pinned_ref>'. This app no longer installs anything from PyPI: our fork's main carries real fixes/features PyPI's official mlx-lm doesn't have (NemotronH MTP self-speculative decode, RotatingKVCache quantization, native prism_hadamard_qwen35 support, --model-alias/--kv-bits/api-v0-models/disconnect-safety server flags). Bump deliberately via LLMTray's Check for Updates action, not by hand -- see the doc comment on RuntimeManager.swift."
}
